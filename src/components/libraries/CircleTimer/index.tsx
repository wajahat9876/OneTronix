/* eslint-disable prettier/prettier */
import {
    CountdownCircleTimer as Lib,
    Props,
} from 'react-native-countdown-circle-timer';

const CountDownCircleTimer = (props: Props) => {
  return <Lib {...props} />;
};

export default CountDownCircleTimer;
