/* eslint-disable import/prefer-default-export */
import { vs } from '@utils/design/design';
import { StyleSheet } from 'react-native';

export const accountTypeExpandedStyle = StyleSheet.create({
  heading: {
    fontSize: 20,
    fontFamily: 'poppins-medium',
    alignSelf: 'center',
    marginTop: vs(16),
  },
});
